<div>
  <h1 class="font-bold text-4xl">My Tweets</h1>

  <%= turbo_stream_from current_account, :feed %>
  <div id="feed">new tweets here</div>
  <% @tweets.each do |tweet| %>
    <%= render 'tweet', tweet: tweet %>
  <% end %>
</div>
