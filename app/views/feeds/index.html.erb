<div>
  <h1 class="font-bold text-4xl">My Feed</h1>

  <%= turbo_stream_from current_account, :feed %>

  <div id="feed">new tweets here</div>

  <% @feed.each do |tweet| %>
    <%= render 'tweets/tweet', tweet: tweet %>
  <% end %>
</div>
